export const template = `
<main>
    <form name="login" >
        <div class="container">
            <h1 class="title-bar title-font">#ЧАТ</h1>

            <div class="container centered-container">
                {{{login}}}
                {{{password}}}
            </div>

            <div class="container centered-container">
                {{{button}}}
                {{{loginError}}}
            </div>
            <div class="container centered-container">
                {{{toRegistration}}}
            </div>
        </div>
    </form>
</main>
`; 